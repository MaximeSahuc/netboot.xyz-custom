#!ipxe

set squash_url http://192.168.0.5:8080/dietpi/filesystem.squashfs
set kernel_url http://192.168.0.5:8080/dietpi/

imgfree
kernel ${kernel_url}vmlinuz ip=dhcp boot=casper netboot=url url=${squash_url} initrd=initrd.magic ${cmdline}
initrd ${kernel_url}initrd
boot